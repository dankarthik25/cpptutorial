/*
    Function call has certain overhead (create, push, pop ,return add& value)

    if there is a simple function the function overhead is move than than execting function

    For this we use inline overhead
        avoid fuction call overhead
        generate inline assembly code
        faster
        larger binary's

    Syntax:
            inline int max(int a,int b):
                return (a>b)? a:b;
                


 */


#include <iostream>

